
# Application name
spring.application.name=orientationApp

# Server port (Render uses PORT environment variable)
server.port=${PORT:8080}

# PostgreSQL datasource
# Use environment variables if present, otherwise fallback to local dev DB
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d4v7ievpm1nc73bib510-a/orientationdatabase}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Christian123}

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
